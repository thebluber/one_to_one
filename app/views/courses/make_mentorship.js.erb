$("#<%= @course.id %>").remove();
$("#user_courses").append("<%= escape_javascript(render(partial: @course, locals: {
  course: @course,
  url: destroy_mentorship_course_path(@course),
  icon_type: 'minus-square',
  method: :delete,
  button: 'Nicht mehr anbieten'})) %>");
