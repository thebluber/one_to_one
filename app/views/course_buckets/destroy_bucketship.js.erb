$("#<%= @course.id %>").remove();
$("#courses").append("<%= escape_javascript(render(partial: @course, locals: {
  course: @course,
  url: make_bucketship_course_bucket_path(@bucket),
  method: :post,
  button: 'Hinzufügen'})) %>");
